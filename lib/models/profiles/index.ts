/**
 * Profiles Module
 *
 * Exports:
 * - Profile schema (table + relations + matching weights)
 * - Profile CRUD operations
 * - Matching engine (client-scoped ANN + weighted ranking)
 */

export * from "./schema";
export * from "./operations";
